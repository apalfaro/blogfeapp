apiVersion: v1
kind: Service
metadata:
  name: blog-frontend-service
  labels:
    app: blog-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: blog-frontend
